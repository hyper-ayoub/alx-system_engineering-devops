#!/usr/bin/env bash
# Script for gracefully restarting Gunicorn

# Send HUP signal to Gunicorn master process
pkill -HUP gunicorn

# Sleep for a short duration to allow workers to gracefully finish ongoing requests
sleep 5

# Restart Gunicorn service
sudo systemctl restart gunicorn
